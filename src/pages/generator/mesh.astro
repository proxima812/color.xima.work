---
import { MeshGenerator } from "@/components/colors";
import { getGeneratorMeshPath } from "@/config";
import { BaseLayout } from "@/layouts";
---

<BaseLayout
	title="Mesh generator - color.xima"
	description="Place your own color points and build a custom mesh gradient, then copy Tailwind, CSS3, or React."
	canonicalPath={getGeneratorMeshPath()}
>
	<MeshGenerator />
</BaseLayout>
